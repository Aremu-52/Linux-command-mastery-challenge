# Day 1 Practice Drill

## Task

I practiced basic Linux filesystem navigation by checking my current location, moving to `/var/log`, returning to my home directory, returning to `/var/log`, and finally displaying its contents in a detailed and human-readable format.

## Commands I Ran

```bash
pwd
cd /var/log
cd ~
cd -
ls -lh
Result

The drill helped me practice moving between directories using both absolute paths and shortcuts. I also confirmed the contents of /var/log using ls -lh.